import React from 'react';

// when a page is not found (incorrect url or wrong course's id in the url)
const NotFound = () => (
  <div className="bounds">
    <h1>Not Found</h1>
    <p>Sorry! We couldn't find the page you're looking for.</p>
  </div>
);

export default NotFound;
