import React from 'react';

// Page displayed when the server returns a error 500
const UnhandledError = () => (
  <div className="bounds">
    <h1>Error</h1>
    <p>Sorry! We just encountered an unexpected error.</p>
  </div>
);

export default UnhandledError;
